name: Crear carpeta de nueva aplicación laboral

on:
  push:
    paths:
      - 'to_process/*.yaml'

permissions:
  contents: write
  issues: write
  repository-projects: write

concurrency:
  group: crear-carpeta-aplicacion-laboral-main
  cancel-in-progress: true

jobs:
  crear-carpeta-aplicacion:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          pip install pyyaml requests

      - name: Instalar pandoc
        run: sudo apt-get update && sudo apt-get install -y pandoc

      - name: Instalar LaTeX (paquetes recomendados)
        run: sudo apt-get update && sudo apt-get install -y texlive-latex-recommended texlive-xetex texlive-fonts-recommended texlive-latex-extra

      - name: Procesar archivo de nueva aplicación
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          for file in to_process/*.yaml; do
            python aplicaciones_laborales/scripts/procesar_aplicacion.py "$file"
          done

      - name: Validar configuración de repositorio destino
        id: check_target_repo
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_APLICACION_LABORAL || secrets.GITHUB_TOKEN }}
        run: |
          TARGET_REPO="angra8410/todas-mis-aplicaciones"
          echo "Verificando si el repositorio destino existe..."
          if [ -n "${{ secrets.PAT_APLICACION_LABORAL }}" ]; then
            echo "🔑 Usando PAT_APLICACION_LABORAL para acceso cross-repo"
          else
            echo "⚠️  Usando GITHUB_TOKEN (puede no funcionar con repos privados)"
          fi
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: token $GITHUB_TOKEN" \
            "https://api.github.com/repos/$TARGET_REPO")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "repo_exists=true" >> $GITHUB_OUTPUT
          else
            echo "repo_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Copiar CV PDF a repositorio todas-mis-aplicaciones
        if: steps.check_target_repo.outputs.repo_exists == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_APLICACION_LABORAL || secrets.GITHUB_TOKEN }}
        run: |
          echo "🔄 Iniciando copia de PDFs al repositorio todas-mis-aplicaciones"
          for folder in to_process_procesados/*/; do
            if [ -d "$folder" ]; then
              folder_name=$(basename "$folder")
              if [[ "$folder_name" != *.yaml ]]; then
                echo "📁 Procesando copia de PDF para: $folder_name"
                python aplicaciones_laborales/scripts/copy_pdf_to_documents_repo.py "$folder_name" || echo "⚠️  Advertencia: No se pudo copiar PDF para $folder_name"
              fi
            fi
          done
          echo "✅ Proceso de copia de PDFs completado"

      - name: Commit y push de cambios
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          if git diff --staged --quiet; then
            echo "✅ Sin cambios para commitear. Workflow completado exitosamente."
            exit 0
          fi
          git commit -m "Automatización: Nueva aplicación laboral creada por workflow"

          # Prevención y manejo de ejecuciones concurrentes con pull/rebase/push seguro
          MAX_RETRIES=3
          RETRY_COUNT=0
          PUSH_SUCCESS=false

          while [ $RETRY_COUNT -lt $MAX_RETRIES ] && [ "$PUSH_SUCCESS" = false ]; do
            echo "📊 Intento de push $((RETRY_COUNT + 1))/$MAX_RETRIES"
            git fetch origin main
            LOCAL_COMMIT=$(git rev-parse HEAD)
            REMOTE_COMMIT=$(git rev-parse origin/main)
            if [ "$LOCAL_COMMIT" != "$REMOTE_COMMIT" ]; then
              echo "⚠️  Se detectaron cambios remotos. Aplicando rebase..."
              if git rebase origin/main; then
                echo "✅ Rebase exitoso"
              else
                echo "❌ ERROR: Conflicto durante el rebase"
                git rebase --abort
                echo "❌ Rebase abortado. Se requiere intervención manual."
                exit 1
              fi
            fi
            if git push origin HEAD:main; then
              echo "✅ Push exitoso!"
              PUSH_SUCCESS=true
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))
              WAIT_TIME=$((5 * (2 ** (RETRY_COUNT - 1))))
              echo "⏳ Esperando ${WAIT_TIME} segundos antes del siguiente intento..."
              sleep $WAIT_TIME
            fi
          done
          if [ "$PUSH_SUCCESS" = false ]; then
            echo "❌ ERROR: No se pudo completar el push después de $MAX_RETRIES intentos"
            exit 1
          fi
          echo "🎉 Workflow completado exitosamente!"
